@model IEnumerable<User>
@{
    ViewData["Title"] = "Find";
}

@foreach(var user in Model)
{
    <a asp-controller="Home" asp-action="SendPrivateMessage" asp-route-userId="@user.Id">
        @user.UserName
    </a>
}

